server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/dental_clinic_management"
    username: root
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true